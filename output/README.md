# Generated analytical outputs

Run `python run_pipeline.py` from the repository root to generate the BI-ready CSV tables, SQLite database, reconciliation reports, scorecards, model diagnostics and predictions here.

Large generated files are intentionally excluded from Git. Executed results are visible in the four notebooks, with key findings in `docs/executive_summary.md` and validation evidence in `docs/validation.md`.
